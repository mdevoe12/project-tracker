<%= form_for [@user, @project, @note] do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :content %>
  <%= f.text_area :content %>

  <%= f.collection_select :statuses, Status.order(:name),:id,:name, include_blank: true %>

  <%= f.submit %>
<% end %>
